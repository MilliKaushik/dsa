package array.math;

import java.util.Arrays;

public class SmallestPositiveIntegerThatIsNotSum {

	long smallestpositive(long[] array, int n) {
		Arrays.sort(array);
		for (int i = 0; i < array.length; i++) {

		}
		return 0;
	}
}