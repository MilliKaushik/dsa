package tree.trie;

import java.util.List;

//https://www.careercup.com/question?id=3088
public class DictionaryAnagramPresent {

	public boolean isAnagramPresent(List<String> strs, String key) {
		// createDictionary(strs); -- create a trie and insert all strings in the list
		// into trie, also add the sorted string of all strings in the trie
		// while checking, get the sorted string and check for the sorted string in the
		// trie
		return false;
	}

}
